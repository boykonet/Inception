version: "3.9"

services:
  mariadb:
    build:
      context: requirements/mariadb/
      dockerfile: Dockerfile
    restart: always
    image: mariadb
    container_name: mariadb
    networks:
      - net
    volumes:
      - ./requirements/tools/mariadb_database:/var/lib/mysql
    env_file:
      - .env
    expose:
      - ${MYSQL_PORT}
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
#    healthcheck:
#      test: ["CMD", ]
#      interval: 1s
#      timeout: 10s
#      retries: 10
#      start_period: 15s

  nginx:
    build:
      context: requirements/nginx/
      dockerfile: Dockerfile
    restart: always
    image: nginx
    container_name: nginx
    networks:
      - net
    volumes:
      - ./requirements/tools/wordpress_database:/var/www/wordpress
    ports:
      - ${HOST_PORT}:${CONTAINER_PORT}
      - ${HOST_PORT_SSL}:${CONTAINER_PORT_SSL}
      - 9000:9000
    environment:
      DOMAIN_NAME: ${DOMAIN_NAME}
      CERTS_PATH: ${CERTS_PATH}
    depends_on:
      - mariadb

  wordpress:
    build:
      context: requirements/wordpress/
      dockerfile: Dockerfile
    restart: always
    image: wordpress
    container_name: wordpress
    networks:
      - net
    volumes:
      - ./requirements/tools/wordpress_database:/var/www/wordpress
    expose:
      - ${WORDPRESS_PORT}
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      DOMAIN_NAME: ${DOMAIN_NAME}
      WORDPRESS_PORT: ${WORDPRESS_PORT}
      MYSQL_CONTAINER_NAME: ${MYSQL_CONTAINER_NAME}
      MYSQL_PORT: ${MYSQL_PORT}
    depends_on:
      - mariadb
      - nginx

networks:
  net:
    name: netinet

#  /home/login/data

#volumes:
#  mariadb_database:
#    name: mariadb_db
#    driver: local
#    driver_opts:
#      type: nfs
#      o: bind
#      device: ./requirements/tools/mariadb_database
#  wordpress_database:
#    name: wordpress_volume
#    driver: local
#    driver_opts:
#      o: bind
#      type: none
#      device: $PWD/requirements/tools/wordpress_database